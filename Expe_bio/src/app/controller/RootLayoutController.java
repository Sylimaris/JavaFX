package app.controller;

import app.MainApp;

/**
 * Classe permettant de gerer le RootLayout consituant le panel superieur de l application
 * @author Thomas LEONARDON et Yassin EL DJOUDI a partir du tutoriel code.makery.ch
 *
 */
public class RootLayoutController {

    /**
     * Is called by the main application to give a reference back to itself.
     *
     * @param mainApp
     */
    public void setMainApp(MainApp mainApp) {
    }
}